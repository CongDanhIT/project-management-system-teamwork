feat(ui): Nâng cấp giao diện Editorial Tech và tối ưu AI Services

- **Frontend (UI/UX):**
  - Áp dụng triết lý "Tinted Glass" vào `Sidebar.tsx`: Thêm Glow Gradient ở góc trên và ẩn thanh cuộn thô cứng (`scrollbar-hide`).
  - Thêm Custom Scrollbar siêu mỏng, sang trọng vào `globals.css` dành cho toàn bộ ứng dụng.
  - Nâng cấp `TaskCard.tsx`: Tối ưu Typography (chữ đậm, tracking-tight) và thêm hiệu ứng nảy (Hover Depth) để tạo cảm giác 3D.
- **Backend (Services):**
  - Sửa lỗi cú pháp Template Literal trong `ai.service.ts` gây lỗi biên dịch.
  - Cải thiện Prompt của Tool `createTask` giúp LLM tự động gán Priority và Status.
- **Tài liệu (Docs):**
  - Bổ sung nguyên lý thiết kế "Tinted Glass" (Ánh sáng môi trường & Kính mờ) vào `DESIGN.md`.
  - Khởi tạo tài liệu phân tích triển khai Giai đoạn cuối (`KeHoachTrienKhai-TintedGlass.md`).
